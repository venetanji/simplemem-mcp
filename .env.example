# SimpleMem MCP Docker Environment Configuration

# Required: API key for your LLM provider (OpenAI, Anthropic, etc.)
API_KEY=your_api_key_here

# Optional: Model name (default: gpt-4)
# Supported models depend on your API provider
MODEL_NAME=gpt-4

# Optional: Override the API endpoint URL
# Default: http://simplemem-api:8000 (container name in docker-compose)
SIMPLEMEM_API_ENDPOINT=http://simplemem-api:8000

# Optional: OAuth tuning
# SIMPLEMEM_OAUTH_TOKEN_EXPIRY_SECONDS=3600
# SIMPLEMEM_OAUTH_JWT_LEEWAY_SECONDS=60

# Optional: Tailscale (used by docker-compose.tailscale.override.yml)
# TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TS_EXTRA_ARGS=--advertise-tags=tag:container

# Optional: Database configuration
# DB_PATH=/app/data/simplemem_data
# DB_TYPE=lancedb
# TABLE_NAME=memories

# Optional: Enable CUDA for GPU acceleration
# USE_CUDA=true
